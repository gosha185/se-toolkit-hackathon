Version 1:
Let's implement in MVP only taking a list of options from user and output of one option with equal chances. An agent will take a list of any format (separated by commas, just space, with greetings at the beginning etc).

Version 2:
Full web app will be also avalible to store user data (login, password, usage history), support choises with different chances, and use the history (like answering on requsets such "chose from options which I gave you on the previous week" or "I need to decide what to eat again" and vary the output of frequently asked requests).
